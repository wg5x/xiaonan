# 后续需求

本目录用于存放后续新来的需求。

规则：

1. 同一个项目的需求都放在本项目目录下，不散放到仓库根目录。
2. 新需求先放在这里，评估后再同步到 `docs/01-prd/` 或 `docs/02-estimates/`。
3. 如果后续需求很多，再在本目录下按主题分组。

建议文件命名：

```text
YYYY-MM-DD-需求名称.md
```

单个需求建议格式：

```markdown
# 需求名称

## 背景

## 需求内容

## 优先级

## 是否进入 MVP

## 周期与成本影响
```
